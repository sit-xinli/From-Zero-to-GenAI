# Core LangChain packages
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-community>=0.0.20

# OpenAI client library
openai>=1.0.0

# Vector Store (FAISS)
# Use faiss-gpu if you have CUDA installed and a compatible GPU
faiss-cpu>=1.7.4

# DuckDuckGo Search Tool
duckduckgo-search>=5.0.0

# For loading environment variables (.env file)
python-dotenv>=1.0.0

# Pydantic for Output Parsing (often a dependency, but good to list)
pydantic>=2.0.0
